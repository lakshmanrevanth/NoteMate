import 'package:flutter/material.dart';

class SearchDelegate extends StatelessWidget {
  const SearchDelegate({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
